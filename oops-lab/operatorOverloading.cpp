#include<iostream>
using namespace std;

class Complex{
    private:
    int real;
    int imaginary;
    public:
    Complex() : real(0), imaginary(0) {}
    Complex(float real, float imaginary) : real(real), imaginary(imaginary){}
    Complex()
}